<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>__LABEL__</string>

    <!-- __LABEL__, __TOOLS_DIR__, __REPO_DIR__ and __HOME__ are all filled in by
         `lc install`; the label is derived from $USER so this file ships with
         nobody's username in it. -->
    <key>ProgramArguments</key>
    <array>
        <string>__TOOLS_DIR__/lc</string>
        <string>push</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__REPO_DIR__</string>

    <!-- git and gh live in homebrew; launchd jobs get a bare PATH otherwise. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        <key>HOME</key>
        <string>__HOME__</string>
        <key>PYTHONUNBUFFERED</key>
        <string>1</string>
    </dict>

    <!-- 23:00 nightly, plus a 09:00 retry so a night spent asleep still lands. -->
    <key>StartCalendarInterval</key>
    <array>
        <dict>
            <key>Hour</key><integer>23</integer>
            <key>Minute</key><integer>0</integer>
        </dict>
        <dict>
            <key>Hour</key><integer>9</integer>
            <key>Minute</key><integer>0</integer>
        </dict>
    </array>

    <!-- A clean tree makes a redundant run a no-op, so firing twice is free. -->
    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>__REPO_DIR__/.logs/sync.out.log</string>
    <key>StandardErrorPath</key>
    <string>__REPO_DIR__/.logs/sync.err.log</string>
</dict>
</plist>
